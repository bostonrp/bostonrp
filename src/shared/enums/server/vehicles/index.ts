
// IMPORTS

import * as fuel from './fuel';

// CODE

const seatNumbers = {
    driver: 0,
    frontLeft: 1,
    backLeft: 2,
    backRight: 3
}

const handlingNames = {
    'fInitialDriveMaxFlatVel': 'fInitialDriveMaxFlatVel'
};

export default {
    fuel,
    seatNumbers,
    handlingNames
}